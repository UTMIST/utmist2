'use client'

import '../styles/globals.css'

export default function AI2Layout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="ai2-app">
      {children}
    </div>
  )
} 